<section>
  <h1>
    <%= render('shared/back') %>
    Create New Category
  </h1>

  <%= form_with(model: @category) do |f| %>
    <div>
      <%= f.label(:name) %>
      <%= f.text_field(:name, placeholder: 'Category Name', required: true) %>
    </div>

    <div>
      <%= f.label(:icon) %>
      <%= f.text_field(:icon, placeholder: 'Category Icon', required: true) %>
    </div>

    <%= f.submit('Create') %>
  <% end %>
</section>
